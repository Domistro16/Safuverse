{
  "name": "safudomains-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for SafuDomains (.id) on Base — registrations, referrals, auctions, reputation",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy safudomains/1.0.0 --path .",
    "deploy:testnet": "goldsky subgraph deploy safudomains-testnet/1.0.0 --path ."
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
